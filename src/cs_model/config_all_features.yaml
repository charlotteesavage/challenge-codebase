data_dir: /SAN/medic/BIC-MAC-CS/bic-mac-data/train #CHANGE to your dataset path
num_workers: 2
output_dir: /SAN/medic/BIC-MAC-CS/bic-mac-data/outputs_5ch
cache_dir: /SAN/medic/BIC-MAC-CS/bic-mac-data/persistent_cache_5ch

epochs: 250
learning_rate: 0.0003
batch_size: 1
seed: 42 # fixes which subjects land in val vs train

# patch_size: [192,192,192]
patch_size: [128,128,128] # reduced patch size for quicker training
train_num_samples: 2 #Number of patches to sample per train subject
val_num_samples: 8 #Number of patches to sample per val subject
